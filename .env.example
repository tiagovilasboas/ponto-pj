# Supabase Configuration
# Obtenha essas credenciais em: https://supabase.com/dashboard/project/[YOUR-PROJECT-ID]/settings/api

# URL do seu projeto Supabase
VITE_SUPABASE_URL=https://your-project-id.supabase.co

# Chave anônima (anon key) - Segura para usar no frontend
VITE_SUPABASE_KEY=your-anon-key-here

# ⚠️  IMPORTANTE: 
# - NUNCA commite o arquivo .env no repositório
# - Use apenas a chave anônima (anon key) no frontend
# - A chave de serviço (service_role key) deve ser usada apenas no backend
# - O arquivo .env já está no .gitignore para evitar commits acidentais 